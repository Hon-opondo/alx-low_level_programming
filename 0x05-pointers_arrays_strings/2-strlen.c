#include "main.h"
#include <string.h>
/**
 * int _strlen - returns the length of a string
 * @s: string pointer
 * Return: returs the length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
