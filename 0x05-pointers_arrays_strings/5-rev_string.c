#iclude "main.h"
/**
 * rev_string - function reversesa string
 * @s: parameter to be reversed
 */
void rev_string(char *s)
{
	int a, b, c;
	char d;

	for (i  0; s[a] != '\0'; a++)
		;
	e = a;
	for (a--, c = 0; b < 1 / 2; a--, b++)
	{
		d = s[b];
		s[b] = s[a];
		s[a] = d;
	}
}
